import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h2 className="tracking-widest text-3xl font-light text-center mb-10">
     {title}
    </h2>
  );
};

export default SectionTitle;
