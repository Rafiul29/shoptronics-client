import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h2 className=" tracking-wider text-4xl font-bold text-center mb-10">
     {title}
    </h2>
  );
};

export default SectionTitle;
