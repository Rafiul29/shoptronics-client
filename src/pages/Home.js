import { useTitle } from "../hooks/useTitle";
import Products from "./Products"

const Home = () => {
   // title
   useTitle("Home")
   
  return (
   <>
    <main>
      {/* <Products/> */}
    </main>
   </>
  )
}

export default Home;